<h1>Personajes</h1>

<div class="row text-center justify-content-center">
    <div class="col">
        <% @characters.each do |a| %>
        <h1 ><%=a.name%></h1>
        <p><%=a.location%></p>
        <p><%=a.house%></p>
       <% end%>
    </div>
  </div>
